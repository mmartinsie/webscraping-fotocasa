# Dependencies for the Streamlit Cloud deployment (webapp/streamlit_app.py).
# Streamlit Community Cloud only reads a requirements.txt at the repository root.
# Mirror of webapp/requirements.txt.
streamlit>=1.31
google-generativeai>=0.8
numpy>=1.26
