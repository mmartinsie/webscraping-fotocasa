# Screenshots for the README

Drop two PNGs here so the main `README.md` renders them:

- `estimator.png` — the static page (https://mmartinsie.github.io/webscraping-fotocasa/)
  with a result shown.
- `agent.png` — the Streamlit LLM agent, Chat tab, with a tool call/result visible.

~1200 px wide, light or dark theme. That's it — the README already links them.
